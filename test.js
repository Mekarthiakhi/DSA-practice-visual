const { interpretCode } = require('./dist/utils/jsInterpreter.js'); const code = \unction slindingWindow(arr) { let k = 4; console.log(arr[k]); let map = new Map(); let avarage = 0; let Sum = 0; let sildWindow = 0; for (let i = sildWindow; i < k; i++) { Sum += arr[i]; avarage = Sum / k; if (i == k - 1) { map.set(sildWindow, avarage); console.log(map); sildWindow++; } } } const result = slindingWindow([1, 12, -5, -6, 50, 3]);\; console.log(interpretCode(code).error || interpretCode(code).steps.length);
